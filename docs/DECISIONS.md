# DECISIONS.md — autopilot decisions log

Every choice made without asking (AUTOPILOT rule 1). One line of rationale each. Items marked
**[client]** need the customer's confirmation; the rest are engineering calls.

| # | Date | Decision | Rationale | Reversible? |
|---|---|---|---|---|
| D-001 | 2026-09-15 | Django **5.2 LTS** + Wagtail **7.0 LTS** (spec said 5.1 / 6.x) | 5.1 and 6.x are end-of-life (no security patches; `pip-audit` fails, contradicts spec §8). wagtail-localize 1.14 hard-requires Django ≥ 5.2 and Wagtail ≥ 7.0. Same API family. See ADR-0001. | Yes (pins in pyproject) |
| D-002 | 2026-09-15 | Compose Postgres published on host port **5433** | Developer machine already runs PostgreSQL 18 on 5432. Inside the network it is still `db:5432`. | Yes |
| D-003 | 2026-09-15 | **Wireframe-only UI** until the designer's Figma arrives (developer instruction) | Design is produced separately. All visual decisions confined to `static/src/tokens.css` + `templates/components/` so the design can be applied without touching Python. Milestone **M5b — Design integration** added. `docs/COMPONENT_INVENTORY.md` (ru+uz) is the hand-off to the designer. | n/a |
| D-004 | 2026-09-15 | PII encryption via an in-house `EncryptedTextField` (`apps.core.fields`) on `cryptography.MultiFernet` | 40 lines, fully typed, key rotation with `PII_ENCRYPTION_KEYS` (newest first) — fewer third-party risks than `django-fernet-fields` (unmaintained). | Yes |
| D-005 | 2026-09-15 | Custom `User` model (`apps.users.User`) and custom image/document models (`media_library.PortalImage` / `PortalDocument`) created in M0 | Must exist before the first migration; adding later forces a painful data migration. | Hard later, hence now |
| D-006 | 2026-09-15 | Celery beat uses the static `CELERY_BEAT_SCHEDULE` (no `django-celery-beat`) | Fewer moving parts for a single operator; schedules are code-reviewed. | Yes |
| D-007 | 2026-09-15 | Wagtail admin mounted at `/cms/` (env `CMS_URL_PREFIX`), Django admin at `/django-admin/` | Spec §4.4 / §8: admin URL must not be `/admin/`. | Yes |
| D-008 | 2026-09-15 | Cloudflare **Turnstile** for anti-spam (not hCaptcha) | Spec allows either; Turnstile is free, privacy-friendly, no puzzle for users. Prod refuses to start without a secret key. **[client]** must register the site key. | Yes |
| D-009 | 2026-09-15 | Full-text search config `ertaaniqla` = `simple` + `unaccent` created by a core migration | Spec §7; the same config serves uz (Latin) and ru text. | Yes |
| D-010 | 2026-09-15 | `DirectoryPage` («Куда обратиться») lives **inside the women's section** as its 4th menu item; the spec's short URLs `/uz/qayerga-murojaat/` and `/ru/kuda-obratitsya/` are Wagtail redirects to it | TZ lists it as a women's-section menu item; one canonical page, menu order preserved, no extra "menu link" model. Children's pages link to it with `?section=children`. | Yes (move page + redirect) |
| D-011 | 2026-09-15 | `embed` / `video` external links are rendered as **provider iframes without oEmbed** (`apps/articles/embeds.py`): YouTube (nocookie), Telegram, Instagram, TikTok only; Instagram/TikTok behind click-to-load | No network at validation/render time (deterministic tests, works offline), no third-party JS on our origin (CSP), exactly the four providers the TZ names. | Yes |
| D-012 | 2026-09-15 | Two emergency banners: site-wide in `SiteSettings` (every page) and `HomePage.emergency_banner` (home only) | Spec lists the banner in both places; both are optional and independent. | Yes |
| D-013 | 2026-09-15 | Care & support / Life after cancer (women) seeded with `show_in_menus=False`; children's versions in the menu | Spec §2.1: women's top menu = 5 TZ items; the two pages stay reachable from the section index and CTAs. Editors can toggle `show_in_menus`. | Yes |
| D-014 | 2026-09-15 | Mobile navigation and accordions use native `<details>`; Alpine only for the embed facade | Works without JS, keyboard-accessible, printable, zero custom JS weight. | Yes |
| D-015 | 2026-09-15 | Reference data (14 regions, 8 services, Wagtail locales uz/ru) created by data migrations, not by `seed_content` | Available in every environment (tests, CI, prod) after `migrate`; the TZ-fixed lists are not editorial content. | Yes |
| D-016 | 2026-09-15 | Search covers **both locales** (visitor language first), synonym groups and the Latin↔Cyrillic table live in code (`apps/search/synonyms.py`, `translit.py`), results merged from FTS + title autocomplete | TZ asks for bilingual discovery; a ru page is better than nothing for a uz query on a thin site; code-managed lists are reviewable in git (CMS-managed list = TODO_HARDENING H-005). | Yes |
| D-017 | 2026-09-15 | OG images are rendered by a Celery task on `page_published` into `BasePage.og_image_generated` (saved with `update_fields`, no new revision); editors can still choose `og_image` manually | Spec §10; no revision noise, regenerated on every publish, works for every page type. Fonts: bundled DejaVu (Latin + Cyrillic + U+02BB, free licence). | Yes |
| D-018 | 2026-09-15 | Video pipeline: original upload is never served; ffmpeg renders 720p/480p H.264 + poster into the default storage; poster becomes a decorative `PortalImage`; tiny sources are not upscaled | Spec §3/§8; single storage abstraction (S3-ready). | Yes |
| D-019 | 2026-09-15 | Consent is enforced in `PatientStoryPage.clean()` only when the page is live/being published; drafts may be saved without consent; the consent document is forced private | Editors can prepare a story before the paper is signed, but nothing can go public without it (F10). | Yes |
| D-020 | 2026-09-15 | Upload hardening: libmagic MIME sniffing for videos/documents/subtitles, WebVTT header check, EXIF/XMP stripped from JPEG/PNG/WebP on upload (orientation applied); ClamAV stays optional (compose profile) | Spec §8. | Yes |
| D-021 | 2026-09-15 | Leaflet is a **separate bundle** (`static/dist/map.js`, 44 KB gz) loaded only on the directory page; `main.js` stays under the 50 KB budget. Markers are circle markers (no image sprites); tiles from OpenStreetMap (already in CSP `img-src`) | Spec §4.6 budgets apply to the global bundle; the map is one page. Self-hosted tile proxy stays optional (M6). | Yes |
| D-022 | 2026-09-15 | Tool wording (result texts, disclaimers, checklist items) lives in **Wagtail pages** (`ScreeningToolPage`, `SelfCheckPage`); only the screening rule table and the scoring live in code; pages are gated by waffle flags `tools_screening` / `tools_selfcheck` (ON in dev/test, OFF in prod until sign-off) | Doctors must own every visible sentence and every checklist item (rule 3); rules are TZ facts. | Yes |
| D-023 | 2026-09-15 | No separate «patient route» tool page: the TZ 4-step route is the `steps` block on «Организация лечения» (printable); the tools index links to it | Avoids two copies of the same medical content for editors. | Yes |
| D-024 | 2026-09-15 | Anti-spam stack: honeypot field + Cloudflare Turnstile (verified server-side, skipped when no key) + django-ratelimit 5 POST/min/IP on every public form; forms are HTMX islands with full-page fallback | Spec §6/§8. | Yes |
| D-025 | 2026-09-15 | `Question` publication is refused without `consent_to_publish` (status silently downgraded to *answered*); contacts purged 90 d after the answer, feedback rows deleted after 180 d — static Celery beat entries | Spec §8 retention; safe default even if a moderator clicks the wrong status. | Yes |
| D-026 | 2026-09-15 | Glossary tooltips: first occurrence of each term (or synonym) in `rich_text` blocks becomes a link to the glossary entry with the definition in `title`; skipped inside links/headings; term list cached per language | Cheap, accessible (link + title, keyboard reachable), no JS. Full popovers can come with the design (M5b). | Yes |
| D-027 | 2026-09-15 | Alpine is the **CSP build** (`@alpinejs/csp`); every expression lives in `static/src/main.js` (`copyButton`, `embedFacade`, `selfCheck`), templates only reference names; htmx indicator CSS moved to `components.css` | Strict nonce-based CSP without `unsafe-eval` (spec §8). Small cost: no inline Alpine expressions in templates. | Yes |
| D-028 | 2026-09-15 | Anonymous page cache = Django middleware (`apps/core/cache.PageCacheMiddleware`), key = version + language + path + query + HX-Request; **version bump** on publish/unpublish/move/delete/settings save instead of `delete_pattern`; CSP nonce swapped on hit; off when `PAGE_CACHE_SECONDS=0` (dev/test), 300 s in prod | One counter invalidates everything in O(1), works on any cache backend, no key scans; nginx micro-cache (M6) stacks on top. | Yes |
| D-029 | 2026-09-15 | Yandex.Metrika is loaded only after the visitor presses «Agree» in the consent banner (`localStorage` `ea-consent`); the id and the privacy page come from `SiteSettings`; no GA/GTM; IP anonymisation on | Spec §8 / A7; decision stays per browser, no consent cookie sent to the server. | Yes |
| D-030 | 2026-09-15 | Share bar: Telegram → WhatsApp → Facebook → copy link; Instagram/TikTok = copy link + downloadable **story image 1080×1920** rendered on publish (`BasePage.story_image_generated`, same Pillow pipeline as OG) | Those two networks have no web share intent; a ready vertical image is what bloggers actually post (spec §10). | Yes |
| D-031 | 2026-09-15 | `MaterialsPage` (blogger kit) lives in `apps/media_library`, one page per language under Home, items = StreamField `material` (audience, section, image, document, short video, caption, hashtags); audience filter via `?audience=` | TZ «материалы для распространения» for both sections on one page; editors manage everything in the CMS, no extra app. | Yes |
| D-032 | 2026-09-15 | Lighthouse CI + pa11y-ci run in a dedicated CI job (`a11y-perf`) against a seeded preview server; the same configs (`lighthouserc.json`, `.pa11yci.json`) work locally via `make lighthouse` / `make pa11y` | Spec §4.6/§9 budgets enforced on every push; local run verified 2026-09-16. | Yes |
| D-033 | 2026-09-16 | nginx = official `nginx:1.27-alpine`; site config is an envsubst **template** (`DOMAIN`, `DOMAIN_ALT`, `STAGING_*`) so prod and staging share one file; **gzip only** (pre-compressed `.gz` from whitenoise via `gzip_static`), brotli deferred (H-015) | No custom nginx build to maintain; brotli saves ~15 % on already small assets. | Yes |
| D-034 | 2026-09-16 | Backups: `pg_dump -Fc` nightly into the `backups` volume + restic snapshot (dump + media, raw video excluded) to an S3/SFTP repository in UZ, 7d/4w/6m; weekly restore test into a scratch **database** on the same server (not a container — the backup container has no docker socket); the M6 gate restored into a scratch container manually | One-person-operable, no docker-in-docker; the scratch DB proves the dump is restorable. | Yes |
| D-035 | 2026-09-16 | Scheduler in the backup container is a bash loop (`backup_cron.sh`), not cron | tini-friendly, logs to stdout, no extra package; times configurable via env. | Yes |
| D-036 | 2026-09-16 | Monitoring = compose profile `monitoring` on the same VPS: Prometheus + Alertmanager (Telegram) + Grafana (SSH tunnel only) + nginx/postgres/redis/node/blackbox exporters; dashboards + alert rules committed as files; config placeholders filled by `sed` at container start | Prometheus/Alertmanager have no env expansion; keeping everything in git beats a separate monitoring host for one operator. Uptime-Kuma/external monitor stays optional (H-016). | Yes |
| D-037 | 2026-09-16 | Staging = second compose project on the same VPS, no public ports; served by the prod nginx as `staging.<DOMAIN>` behind basic auth; every push to `main` deploys there, prod needs GitHub environment approval | Spec §11.3; one certificate, one nginx, no port clash. | Yes |
| D-038 | 2026-09-16 | Postgres client in the image pinned to **16** from PGDG (Debian trixie ships 17, whose dumps set `transaction_timeout` that a 16 server rejects) | Found by the restore gate; client must match the server major. | Yes |
| D-039 | 2026-09-16 | TLS bootstrap: `init_letsencrypt.sh` installs a self-signed placeholder so nginx starts, then requests the real certificate (webroot); renewal by the `certbot` service, reload by `nginx-reloader` (docker socket read-only) | Standard chicken-and-egg fix; no manual reload after renewals. | Yes |
