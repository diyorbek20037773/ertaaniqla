# Optional IP allow-list for /cms/ (spec §8). Default: open (2FA + axes protect the login).
# To restrict, replace with e.g.:
#   allow 213.230.0.0/16;   # office
#   allow 10.8.0.0/24;      # VPN
#   deny all;
allow all;
