# htpasswd entries for https://staging.<DOMAIN> (basic auth). Empty = nobody can log in.
# Add a user:  docker run --rm httpd:2.4-alpine htpasswd -nbB editor "s3cret" >> docker/nginx/staging.htpasswd
# Point STAGING_HTPASSWD in .env at your real file (this example denies everyone).
